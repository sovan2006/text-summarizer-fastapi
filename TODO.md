# TODO - Push to GitHub

- [ ] Add `TextSummarizerApplication/results/` to `.gitignore` so large model artifacts are not tracked.
- [ ] Ensure `results/` is removed from git index (keep local files only).
- [ ] Commit the cleanup.
- [ ] Run a single `git push -u origin main` (do not run multiple pushes in parallel).
- [ ] Verify remote has the expected commit.

